node_modules/
dist/
*.log
.DS_Store

# harness runtime (per-worktree state; DB lives at repo root)
.harness/
harness.db
harness.db-*

# harness-cli binary: a build artifact regenerated by scripts/bootstrap-harness.sh
scripts/bin/

# Local-only tooling and working docs — kept on disk, never pushed.
# Anchored to the repo root so package-level scripts/ and docs/ still ship.
/.agents/
/.claude/
/scripts/
/docs/
/AGENTS.md
/CLAUDE.md
